apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: contract-monitor
  namespace: develop
  labels:
    release: kube-prometheus-stack
spec:
  selector:
    matchLabels:
      app: rf-contract-svc
  endpoints:
    - port: http
      path: /api/contract/v1/actuator/prometheus
      interval: 15s
      scrapeTimeout: 10s